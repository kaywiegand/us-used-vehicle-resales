[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dsc_gebrauchtwagen"
version = "0.1.0"
description = "Data Science Projekt mit sauberer Struktur"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]